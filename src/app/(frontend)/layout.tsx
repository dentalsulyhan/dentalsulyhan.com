import '../frontend.css'
import React from 'react'

type FrontendRootLayoutProps = {
  children: React.ReactNode
}

export default function FrontendRootLayout({ children }: FrontendRootLayoutProps) {
  return children
}
